============
Installation
============

At the command line::

    $ pip install ceilometer-ecs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ceilometer-ecs
    $ pip install ceilometer-ecs
